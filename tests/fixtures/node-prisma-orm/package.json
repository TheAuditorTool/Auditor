{
  "name": "node-prisma-orm-fixture",
  "version": "1.0.0",
  "description": "Prisma ORM fixture for testing schema extraction, relationships, transactions, and taint flows",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "7.0.0"
  },
  "devDependencies": {
    "prisma": "7.0.0"
  }
}
