{
  "name": "node-sequelize-orm-fixture",
  "version": "1.0.0",
  "description": "Sequelize ORM fixture for testing model extraction, relationships, hooks, validations, and transactions",
  "scripts": {
    "migrate": "sequelize-cli db:migrate",
    "seed": "sequelize-cli db:seed:all"
  },
  "dependencies": {
    "sequelize": "^6.37.7",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.3"
  }
}
